using TKZ.Shared;

namespace TKZ.Server.Calculation
{
    public static class Calculation
    {
        public static ResultCalc Calc (Grid grid, int BusId)
        {
            ResultCalc res = new ResultCalc (grid);
            // TODO: Long calculation
            return res;
        }
    }
}